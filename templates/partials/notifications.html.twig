{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="alert alert-{{ label }} m-0">
            {{ message|raw }}
        </div>
    {% endfor %}
{% endfor %}
