<div class="card">
    <div class="card-header">
        <h1 class="card-title m-0 h3">
            {{ items is defined ? items|length : '' }} {{ title }}
        </h1>
    </div>
    <div class="card-body">
        {% block body %}{% endblock %}
    </div>
</div>